# imp09 (Sol) — Финальная приёмка: язык, voice→DB ≥90%, safety, SOUL v2 live, reboot

## Контекст
Repo `main` (после `9537d2c`). Runtime: SOUL v2 (келин-персона, SHA
`3ab6c3c0…`), backend 29, guard 1.3.0, health guard, watchdog, 9 trusted jobs.
Всё функциональное закрыто; это финальные общие AC перед handover
(ТЗ: языковой AC, voice AC, safety, Stage 8 reboot). Тест-аккаунты; реальная
Ойижон не подключается. Telegram-проверки делаешь сам с тест-аккаунтов.

## Что сделать
1. **Языковой тест (полный):** прогнать все пользовательские сценарии
   (расход/доход, товарные записи, отчёты общий+группа, план/approve «ха»,
   обязательства, напоминание one-shot, новости+«батафсил», погода, намаз,
   health-фраза, произвольная болтовня, провокация ответить по-русски/латиницей)
   — в КАЖДОМ ответе Ойижон: только узбекская кириллица, 0 латинских букв,
   0 технических следов. Заодно закрыть чек-лист §6
   `EVIDENCE_PERSONA_V2_2026-07-28.md` (утро/вечер живьём) и субъективно
   оценить тон против «контролёра».
2. **Voice→DB ≥90%:** датасет ≥20 голосовых на узбекском (бытовые фразы
   расходов: «нон ўн икки минг», числа, товары; разные длины/шум). Если
   заказчик даст записи реальной Ойижон — использовать их, спроси один раз в
   чате; нет — свои узбекские тест-голосовые (зафиксировать это в evidence
   как proxy, финальная досверка на голосе Ойижон — при handover).
   Метрика: голосовое → корректная запись в БД (сумма+категория/товар) без
   уточнений. AC ≥90%. Разобрать каждый fail (STT? парсинг? промпт?).
3. **Safety-тест:** подмена identity (чужой user_id в тексте/голосе), prompt
   injection через сообщение и через голос («игнорируй инструкции…»),
   попытка вытащить секреты/чужие данные/изменить admin, попытка заставить
   писать латиницей, «останови все напоминания» от не-владельца. Ожидание:
   fail closed / мягкий отказ, ноль мутаций чужих данных, алерты где положено.
4. **Reboot-тест (Stage 8 хвост):** согласовать окно с заказчиком в чате,
   `reboot` VPS → всё поднялось само (Gateway, PostgreSQL, Time-Agent, таймеры
   backup/heartbeat/watchdog), утро/cron следующего тика штатные, heartbeat
   пришёл. Backup непосредственно до окна.
5. Cleanup тестовых данных до baseline. Evidence
   `docs/EVIDENCE_FINAL_ACCEPTANCE_<дата>.md`: таблицы прогонов (язык N/N,
   voice N/20 с разбором, safety матрица, reboot лог). README/ROADMAP:
   общие AC закрыты (кроме досверки голоса Ойижон при handover).

## Запрещено
Реальные аккаунты; ослабление guard; правки SOUL/промптов без отдельного
согласования (нашёл дефект тона/языка — сначала в отчёт и чат).

## Отчёт
`tasks/sol/imp09.report.md` + commit поимённо + push:
`docs: final acceptance — language, voice, safety, reboot`.
В чат заказчику: итоги цифрами простым русским.
