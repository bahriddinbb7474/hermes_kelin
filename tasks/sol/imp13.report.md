# imp13 — отчёт о передаче

Дата: 2026-08-02. Результат: **PASS — семейный профиль передан реальной Ойижон**.

## Backup

До любых изменений создан приватный backup:

```text
/home/timeagent/.hermes/profiles/mariyam_oyijon/backups/imp13-20260802T145923Z
```

Каталог имеет mode `0700`, файлы — `0600`. Контрольные суммы SHA-256:

| файл | SHA-256 |
|---|---|
| `database.dump` | `d34c50593254fcc9e0c592c4cddd5eae716c6915840684be70278956d1ec2431` |
| `state.db` | `3c76983714126742c88eb928011eee2a9feda89c0e916566b913b34ca5ef4d78` |
| `MEMORY.md` | `ceee6ea9acab6defb55cc98e37669fbf21fc8f342f9b123d2ec5ed689a471efb` |
| `USER.md` | `fcc76ea7171690d27d26b77380ae6dc7358f8b826c847c35d3f37df0eba1a942` |
| `SOUL.md` | `5efff19dbbf824ba9d3736339e70dd0c8f514f5ad6e48e1fe12e1296d9bf5125` |
| `identity-map.json` | `5fd31f92761beb4ab29bd4c409bd738e4998f9e37f9c71ed7d7489de39246fc0` |

Перед изменениями зафиксирован эталон: августовские расходы — `1 712 208` сум,
`44` записи; обязательства — `9`; планы августа и сентября сохранены отдельными
SHA-256-отпечатками без вывода персональных финансовых строк.

## SOUL, ленты, память и сессии

Актуальный SOUL установлен в `mariyam_oyijon` и `mariyam_test`. Оба `config
check` прошли; фактический SHA обоих файлов:

```text
f3377d9c0e032127cd5675408b525437a39b36ba9d148ab1f594e84c889aa679
```

Права обоих файлов — `0444`.

Точечно отключены и удалены две тестовые ленты заказчика:

- `https://habr.com/ru/rss/articles/`;
- `https://openai.com/news/rss.xml`.

Пользовательский новостной кэш сброшен. Четыре штатные ленты остались активны.

`MEMORY.md` и `USER.md` сформированы заново из переданной приватной анкеты.
Сохранены структура семьи, семейный финансовый цикл, кредитная схема,
продуктовые нормы, регулярные напоминания и правило сначала предлагать готовый
план. Удалены интересы, манера общения, история диалогов и предпочтения прежнего
тестового собеседника; он упоминается только в семейной и административной роли.
Персональные имена, даты рождения и индивидуальные суммы в этот отчёт не
переносились. Активных сессий в профиле не оказалось; проверка Hermes подтвердила
нулевой набор для сброса, поэтому старый system prompt не может быть продолжен.

## Перепривязка и проверка

Gateway был остановлен только на время финальной перепривязки. У существующей
записи `users.id=20` обновлены Telegram-привязка и отображаемое имя; запись не
пересоздавалась, поэтому все данные сохранили прежний `user_id`. В
`identity-map.json` прежний ключ атомарно заменён новым, mode файла — `0600`.
Роль администратора у `users.id=1` сохранена, второй записи с ролью `oyijon`
нет. После этого gateway запущен.

Итоговые проверки:

1. **PASS** — расходы августа: `1 712 208` сум, `44` записи.
2. **PASS** — обязательства: `9` строк.
3. **PASS** — планы августа и сентября совпали с предпередаточными SHA;
   соответственно `9/19` и `8/19` строк категорий/товаров.
4. **PASS** — пользовательских лент `0`, штатных активных лент `4`.
5. **PASS** — семейные и финансовые сведения в памяти сохранены, сведения о
   личности прежнего тестового собеседника отсутствуют.
6. **PASS** — `busy_ack_enabled=false`, `long_running_notifications=false`,
   `memory_notifications=off`, `tool_progress=false`, `session_reset=daily/02:00`
   без уведомления; единственный fallback — Luna через OpenRouter;
   `mariyam_outbound_filter` включён, версия `1.0.2`; migration 006 активна;
   inventory `TOOLS/DISPATCH/list_tools = 30/30/30`; оба SOUL имеют требуемый
   SHA и mode `0444`.

Сервис после передачи:

```text
ActiveState=active
SubState=running
ExecMainStatus=0
NRestarts=0
```

Сообщения и голосовые проверки в Telegram не выполнялись. Backend, migrations,
tools, guards, cron, конфигурация и исходные данные вне перечисленных шагов не
менялись.

## Git

SOUL-правки: `66fe5ab` и `096254a`. Коммит отчёта и подтверждение push будут
добавлены после фиксации этого файла.
