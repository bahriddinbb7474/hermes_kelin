# fix01 (Opus) — Technical traces в cron-доставке Ойижон + добить live E2E 5.3A

## Контекст
После imp02: 5 production jobs active (paused/resumed — проверить фактическое
состояние), guard 1.2.0, backend 24. Live E2E 25/27/28 помечен PASS, но на
скрине заказчика сообщение job 27 пришло тест-Ойижон в обёртке:
«Cronjob Response: mariyam_plan_27_reminder (job_id: 6c37a7e4ec04)»
и футер «To stop or manage this job, send me a new message (e.g. "stop
reminder ...")». Это нарушение AC «technical traces: 0» + Ойижон может
случайно остановить job. В evidence обёртка не зафиксирована.

## Что сделать
1. Выяснить источник обёртки по коду Hermes v0.18.2 на VPS:
   добавляет ли её только ручной `cron run` (которым делался E2E) или и штатное
   срабатывание по расписанию. Зафиксировать факт с ссылками на код.
2. Убрать обёртку из доставки Ойижон допустимыми средствами (конфиг job/profile,
   deliver-параметры; Hermes core не менять). Если штатный тик уже чистый, а
   обёртку даёт только ручной `cron run` — зафиксировать это доказательно
   (лог штатного срабатывания) и признать live-методику E2E: дальнейшие
   проверки делать штатным срабатыванием или доказанно чистым способом.
3. Перепроверить live доставку job 27 без обёртки (чистая узбекская кириллица).
4. Добить оставшиеся человеческие шаги E2E (координируясь с заказчиком в чате):
   «ха»-approve от тест-Ойижон → `approved_by_oyijon`;
   live 1a (auto_approved + сообщение Ойижон) и 1b (fallback → admin) —
   контролируемым запуском на тестовом месяце, затем cleanup до baseline.
5. Evidence обновить (обёртка: причина, фикс, доказательство чистой доставки;
   результаты «ха»/1a/1b), статусы README/ROADMAP: если всё PASS —
   5.3A = CLOSED / LIVE PASS.

## Запрещено
Hermes core; данные admin; расходы/transactions; секреты в git/логи.

## Отчёт
`tasks/opus/fix01.report.md` + commit(ы) поимённо + push. В чат — простым
русским: причина обёртки, что исправлено, итог E2E.
