# imp04 (Opus) — отчёт. Фаза 1: аудит токенов и личности

Статус: **фаза 1 закрыта, ждём утверждения заказчика. Runtime не менялся.**
Документ: `docs/AUDIT_TOKENS_PERSONA_2026-07.md`.

## Что сделано

1. Измерен реальный расход токенов по `state.db` профиля Hermes на VPS
   (таблица `sessions`: `input_tokens`, `api_call_count`, `cache_read_tokens`).
2. Разобран состав промпта на живом профиле
   (`build_system_prompt_parts` + реальный wire-dump запроса с 47 tools).
3. Точный подсчёт токенов `tiktoken/o200k_base` — не оценка по символам.
4. Разобраны SOUL по разделам, cron-промпты, код новостей.
5. Составлен план компактизации и проект личности «живой келин» с примерами.

## Главные цифры

- Постоянный префикс каждого запроса: **≈ 24 900 токенов**.
  - SOUL — 8 244 (33 %); встроенные tools Hermes — 9 807 (39 %);
    MCP бэкенда 29 шт. — 4 239 (17 %); ядро Hermes — ≈ 2 600 (10 %).
- **7 255 токенов (29 % каждого запроса)** — tools, которые Мариям не
  использует никогда: браузер, файлы, делегирование, session_search,
  картинки, TTS, vision, todo, clarify.
- На личность в SOUL — **172 токена из 8 244 (2 %)**.
- Факт из биллинга Hermes: Telegram — 110 API-вызовов, 2 036 933 входных
  токена (≈ 18,5 тыс./вызов); одно сообщение Ойижон ≈ 40 тыс. входных токенов.
- Прогноз при 40 сообщ./день: **$13/мес** при тарифе $0,20 за 1 млн и
  **$31/мес** при $0,50. После компактизации — **$6 и $14**.
- Тариф n1n из конфига не читается (ключ) — нужен от заказчика.

## План компактизации (эффект −55 %: 24 900 → ≈ 11 100)

1. `disabled_toolsets` += browser, file, delegation, session_search,
   image_gen, vision, tts, todo, clarify → −7 255 (риск низкий).
2. SOUL v2 8 244 → ≈ 2 800 → −5 400 (Stage 5.3-контракты и onboarding —
   в cron-промпты; §5.1 и nutrition-search — удалить как дубль/мёртвое;
   identity и медицину сжать, т.к. их гарантируют плагины).
3. Убрать из чата Ойижон 5 админ-tools → −414.
4. Сжать раздутые описания 4 MCP-tools → ≈ −700.

## Личность — диагноз

Чек-лист-допрос «харажат / Қуръон / соғлиқ» записан дословно в
`cron/06_evening.md` п.3 — бот выполняет инструкцию буквально.
Плюс: 97 % SOUL — форматы и запреты; нет требования вариативности;
§3.2 требует завершать отчёт дословной фразой и не задавать второй вопрос;
утренний промпт — бюллетень из 5 блоков.
В документе — 10 правил Мариям v2 и 9 примеров сообщений на узбекской
кириллице (утро, вечер, расход, поддержка, похвала, планы, новости
с «батафсил айтайми?»).

## Новости

`backend/external_data.py` сохраняет из RSS **только заголовок** — текста нет,
поэтому «подробнее» невозможно, а пересказ выходит в одну строку.
Кандидатов 12, отбора по интересам нет, кэш на сутки.
План: отдавать `description` из RSS, 20–25 кандидатов, отбор и предложение
подробностей — на стороне Hermes, предпочтения — через `memory`.

## Побочные находки

1. **PID 627910 сжигает целое ядро VPS 14 суток** (`python - /opt/hermes-mariyam
   …/plugins/mariyam_identity_guard`, состояние R, load average 1,00 — это он).
   Нужно разрешение, чтобы убить и найти источник.
2. `/opt/hermes-mariyam` — устаревшая копия репо; боевой SOUL живёт в профиле
   Hermes и совпадает с git (sha256 `999b6571…31b60`).
3. `web_search` недоступен, хотя onboarding обещает поиск в интернете.
4. Источники новостей пишутся латиницей (`Kun.uz`) — нарушение правила
   «0 латинских букв» в ответах Ойижон.

## Проверка на месте (что и чем)

- `state.db` — read-only SQLite-агрегаты, тексты сообщений не выгружались.
- Из wire-dump извлечены **только схемы tools**, без данных пользователя.
- Файлы конфигурации с секретами не читались.
- Изменений на VPS не делалось.

## STOP

Нужны от заказчика: (1) утверждение личности и примеров §7 документа,
(2) утверждение списка сокращений §5, (3) тариф n1n, (4) разрешение убить
зависший процесс. После этого — фаза 2.

---

# Фаза 2 — SOUL v2 и компактизация (2026-07-28)

Evidence: `docs/EVIDENCE_PERSONA_V2_2026-07-28.md`.
Статус: код, промпты и тесты готовы и запушены; **боевой деплой не выполнен —
запись в production-профиль требует разрешения заказчика** (команда в §5
evidence).

## Сделано

1. **Токены −45 %**: префикс каждого запроса 24 900 → ≈ 13 800 (o200k).
   - отключены 9 неиспользуемых toolsets (browser, file, delegation,
     session_search, image_gen, vision, tts, todo, clarify) — −7 255;
   - SOUL 8 244 → 4 801 ток. — −3 443;
   - сокращены раздутые описания 4 MCP-tools — −196.
   - `cronjob` и `memory` оставлены: напоминания и предпочтения.
2. **SOUL v2**: первым разделом идёт личность — 10 правил тона (один вопрос в
   сообщении, ротация тем, вариативность формулировок, предлагать а не требовать,
   хвалить, помнить вчерашнее, «батафсил айтайми?»). Контракты отчётов и плана
   сохранены дословно там, где их ломали живые баги (заголовки таблиц, финальная
   фраза, порядок summary → товары, `items: []` запрещён, «ха» → цикл плана).
   Удалены мёртвые директивы: nutrition web search (инструмента нет), дубль
   cron-шагов, тексты onboarding (job не существует).
3. **Cron-промпты**: вечерний чек-лист «расход + Коран + здоровье» заменён на
   одну тему в день по ротации (и похвалу вместо вопроса, если данные уже есть);
   утренний — тёплый, 2–3 релевантные новости с предложением подробностей,
   один вопрос в конце; 25/27 — сначала «ҳол сўра», без давления.
4. **Новости**: backend отдаёт `summary_ru` из RSS и 20 кандидатов вместо 12 —
   теперь «батафсил айтайми?» есть чем ответить. Отбор остался в Hermes.
5. **Regression**: 290 passed, 87 skipped. Обновлены маркеры инспектора,
   контрактные тесты 5.1/5.2/5.3, sha-пин SOUL.
6. **Deploy-инструменты**: `deploy/imp04_deploy.sh` (dry-run/apply/rollback),
   `imp04_patch_config.py`, `imp04_job_id.py`,
   `imp04_refresh_cron_fingerprints.py` (пересчёт trusted fingerprints
   функциями самого guard-а, mapping атомарно, mode 0600, состав записей не
   меняется). Dry-run на VPS выполнен успешно.
7. **Побочное**: убит зависший процесс, 14 суток жёгший ядро VPS.

## Честный итог по деньгам (тариф $1/$6 за 1 млн)

| Нагрузка | Было | Стало |
|---|---:|---:|
| 40 сообщ./день | ≈ $60/мес | ≈ $35/мес |
| 20 сообщ./день | ≈ $32/мес | ≈ $19/мес |
| 10 сообщ./день | ≈ $18/мес | ≈ $11/мес |

Компактизация даёт −45 %, но бюджета $10–15 при 30–40 сообщениях в день она
одна не даёт. Решение за заказчиком:
- перевести основную модель на `deepseek/deepseek-chat` (уже стоит fallback-ом;
  по фактическим openrouter-сессиям вход ≈ $0,20 за 1 млн — в 5 раз дешевле) —
  это ≈ $10/мес при 40 сообщениях, но нужна живая проверка узбекской кириллицы;
- узнать тариф n1n на cache read (кэш префикса уже работает на 29 % входа);
- сузить список MCP-tools в диалоге (ещё ≈ 1 200 токенов на вызов).

## Что осталось

- `--apply` деплой + live acceptance по чек-листу §6 evidence;
- замер «после» по `state.db` за сутки после деплоя.
