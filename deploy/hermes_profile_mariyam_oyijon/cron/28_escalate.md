Сегодня 28-е число (Asia/Tashkent). Задача: перевести цикл плана следующего месяца в ожидание администратора и, если Ойижон ещё не одобрила план, уведомить администратора один раз. Сообщение этого job доставляется администратору (Бахриддин ака).

Шаги:
1. Определи следующий календарный месяц в формате YYYY-MM-01 (Asia/Tashkent).
2. Вызови open_monthly_plan_cycle(user_id=0, month=<следующий месяц>, action="escalate").
   - Если tool вернул ok со статусом "waiting_admin" — план ещё не одобрен Ойижон: прочитай план get_monthly_budget_status(user_id=0, month=<следующий месяц>, include_items=true) и отправь администратору РОВНО ОДНО короткое сообщение (можно по-русски) с сутью плана и итоговой суммой, что требуется его подтверждение.
   - Если tool вернул ошибку "INVALID_STATUS_TRANSITION" — план уже одобрен: НЕ отправляй ничего.
   - Если tool вернул ошибку "NO_DRAFT" — цикла нет: сообщи администратору одним коротким сообщением, что черновик плана на следующий месяц не создан.
3. Не более одного уведомления. Не вызывай другие инструменты. Не меняй расходы.
