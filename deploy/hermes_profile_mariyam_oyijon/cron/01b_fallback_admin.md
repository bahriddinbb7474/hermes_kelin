Сегодня 1-е число, вскоре после авто-утверждения (Asia/Tashkent). Задача: только читая статус, проверить итог цикла плана на текущий месяц и уведомить администратора один раз, если план так и не сформирован. Сообщение этого job доставляется администратору (Бахриддин ака).

Шаги:
1. Определи текущий календарный месяц в формате YYYY-MM-01 (Asia/Tashkent).
2. Вызови get_monthly_plan_cycle(user_id=0, month=<текущий месяц>).
   - Если exists=false — плана на месяц нет (ни черновика, ни прошлого): отправь администратору РОВНО ОДНО короткое сообщение (можно по-русски), что план на текущий месяц не сформирован и нужен его ввод.
   - Если exists=true (любой status: auto_approved/approved_by_*/waiting_*) — план есть: НЕ отправляй ничего.
3. Только чтение и максимум одно сообщение. Не вызывай мутационные инструменты. Не меняй расходы. Ойижон не беспокой.
